services:
    # app:
    #   build: .
    #   env_file:
    #     - .env
    #   ports:
    #     - "5000:5000"

    #   depends_on:
    #     - db

    db:
        image: postgres:16

        environment:
            POSTGRES_DB: k8sreport
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres

        ports:
            - '5432:5432'
